# DBMS---Gram-Panchayat-

# How to setup 

    1) Create a new folder wherever you want and follow the steps http://chatgpt.com/c/67be0c5f-e074-8008-b24c-110162f4f951
    2) It will be better if you create your own branch and push changes into that (keep branch name as your own name )

# Where to work 
    1) For frontend --> create required html files in templates folder and css files(if required) in static folder  
    2) For Backend --> a) if you want to add sql queries, create *.py file in queries folder and write your sql query in that 
                       b) add the corressponding route in app/routes.py/ file and write the necessary code (i guess it will be just ^C + ^V) 

# Tasks assigned 
    According to me, the main part of assignment is just design and handle queries elegantly by following good schema 
    !! Design should be extensive 
    @Ashish, @Shshank, @Sujan will mainly work on this design part and writing corresponding SQL queries + {adding routes (I will take care it )} 
    @Gaurang(or I should say ChatGPT ) will handle the frontend and fill the folders {templates and static} 

# What can you run now 
    1) After setting up the project make sure all the dependencies are installed ( I mean python libraries )
    2) just run --> python run.py, flask server starts running at localhost 
    3) go to localhost/citizens you will see result of query in queries/citizens_queries.py file 
    4) thats it !!

    I think we have to just extend this to make a website + database using form based interface

# Challenges faced 
