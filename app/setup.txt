sudo apt update

sudo apt install postgresql postgresql-contrib

sudo systemctl start postgresql
sudo systemctl enable postgresql

sudo systemctl status postgresql

sudo -i -u postgres

psql

CREATE DATABASE gram_panchayat;
CREATE USER your_username WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE gram_panchayat TO your_username;


Create a database.ini File:

Create a database.ini file in the same directory as your script with the following content:

ini
Copy
[postgresql]
host=localhost
dbname=gram_panchayat
user=your_username
password=your_password
port=5432

python db.py

HOW TO CHECK if working 

Switch to the postgres user (the default PostgreSQL superuser):
sudo -i -u postgres
Access the PostgreSQL prompt:


psql
Now you can connect to the gram_panchayat database:

\c gram_panchayat
List tables and view data as needed:


\dt
SELECT * FROM citizens;
